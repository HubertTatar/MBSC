spring:
  application:
    name: user
  profiles:
    active: dev
eureka:
  client:
    serviceUrl:
      defaultZone:  http://localhost:8000/eureka/
    instance:
      leaseRenewalIntervalInSeconds: 3
    metadata-map:
      management.port: ${management.port:8082}